const POSTGRES = {
  HOST: 'localhost',
  USERNAME: 'postgres',
  PASSWORD: 'postgres',
  DB: 'test',
  PORT: 5432,
  DIALECT: 'postgres',
};

const Constants = {
  POSTGRES
};

module.exports = Constants;
